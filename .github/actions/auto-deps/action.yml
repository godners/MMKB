name: "[ACTION] Auto Dependencies"
# .github/actions/auto-deps/action.yml

description: '使用 Apt 安装 Bash 依赖，使用缓存安装 Python 依赖'

runs:
  using: composite
  steps:       
    - name: "[STEP] Install JQ via Action" 
      id: os-jq
      uses: dcarbone/install-jq-action@main
    
    - name: "[STEP] Set Pip-Cache via Action"
      id: python-cache
      uses: actions/cache@main
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('.github/actions/pip-dependencies/requirements.txt')}}
        restore-keys: |
          ${{ runner.os }}-pip-
    
    - name: "[STEP] Install Dependencies via Command"
      id: python-requirements
      shell: bash
      run: pip install -r ${{ github.action_path }}/requirements.txt

    - name: "[STEP] Print Summary via Action"
      id: bot-summary
      uses: ./.github/actions/bot-summary
      with:
        title: "Auto Dependencies"
        message: "Installed [BASH] jq, [PYTHON] json5."
