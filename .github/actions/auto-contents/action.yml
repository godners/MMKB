name: "[ACTION] Auto Contents"

description: '自动生成 CONTENTS.md 文件'

runs:
  using: composite
  steps:
    - name: 运行 Python 脚本生成所有 CONTENTS.md
      run: python ${{ github.action_path }}/auto-contents.py
      shell: bash
      env:
        ACTION_PATH: ${{ github.action_path }}