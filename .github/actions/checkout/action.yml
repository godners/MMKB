name: 00. Auto Commit

description: 检出仓库代码

runs:
  using: composite
  steps:
    - name: Run Checkout
      uses: actions/checkout@main
      with:
        fetch-depth: 0
        token: ${{ github.token }}