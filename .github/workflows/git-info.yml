name: "[WORKFLOW] Git Info"

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  git-info:
    runs-on: ubuntu-latest

    steps:
      # - name: Checkout repository
      #   uses: actions/checkout@main
      #   with:
      #     fetch-depth: 0
      #     token: ${{ secrets.GITHUB_TOKEN }}

      # - name: Run Git Checkout via Action
      #   uses: ./.github/actions/git-checkout
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: temp apt dep
        uses: ./.github/actions/apt-dependencies

      - name: Run Git Info via Action
        uses: ./.github/actions/git-info
        
      - name: Verify Git Config
        run: git config --local --list
        shell: bash
        